import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'prueba 1 julio herrera',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
